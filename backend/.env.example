# Copy to .env to override defaults. All keys are prefixed MFA_.
# MFA_ENVIRONMENT=development
# MFA_DATA_DIR=./data
# MFA_DATABASE_URL=postgresql+psycopg://user:pass@localhost/mfa
# MFA_MAX_UPLOAD_MB=50
